package Model;

import javafx.scene.image.Image;

public class BasicZombies extends Zombie{

    public BasicZombies(int healthLevel, Power advanceSpeed, Power damagePower, Image animationFrames) {
        super(healthLevel, advanceSpeed, damagePower, animationFrames);
    }
}
