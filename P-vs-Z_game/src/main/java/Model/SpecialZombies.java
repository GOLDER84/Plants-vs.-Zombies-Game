package Model;

import javafx.scene.image.Image;

public class SpecialZombies extends Zombie{

    public SpecialZombies(int healthLevel, Power advanceSpeed, Power damagePower, Image animationFrames) {
        super(healthLevel, advanceSpeed, damagePower, animationFrames);
    }
}
