package Model;

import javafx.scene.image.Image;

public class ResistantZombies extends Zombie{

    public ResistantZombies(int healthLevel, Power advanceSpeed, Power damagePower, Image animationFrames) {
        super(healthLevel, advanceSpeed, damagePower, animationFrames);
    }
}
